ktor {

    development = true

    deployment {
        port = 8080
        port = ${?PORT}
        callGroupSize = 8
    }
    
}

postgresql {
    host = ${?host}
    port = ${?port}
    user = ${?user}
    password = ${?password}
    database = ${?database}
}